<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:padding="16dp"
    android:background="@color/colorPrimaryMid">

    <!-- Activity Title -->
    <TextView
        android:id="@+id/fragment_activitytracker_activity_activityTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Activity Name"
        android:textSize="24sp"
        android:textColor="@color/colorPrimary"
        android:textStyle="bold"
        android:gravity="center"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Total Time Spent -->
    <TextView
        android:id="@+id/fragment_activitytracker_activity_totalTimeTextView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Total Time: 0 hrs 0 mins"
        android:textSize="16sp"
        android:textColor="@color/colorPrimary"
        android:gravity="center"
        app:layout_constraintTop_toBottomOf="@id/fragment_activitytracker_activity_activityTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="8dp"/>

    <!-- RecyclerView for Sessions -->
    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipeRefreshLayout"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/fragment_activitytracker_activity_totalTimeTextView"
        app:layout_constraintBottom_toTopOf="@id/fragment_activitytracker_activity_addSessionButton"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:clipToPadding="false"
        android:paddingBottom="16dp">
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/fragment_activitytracker_activity_sessionsRecyclerView"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_marginTop="16dp"
            app:layout_constraintTop_toBottomOf="@id/fragment_activitytracker_activity_totalTimeTextView"
            app:layout_constraintBottom_toTopOf="@id/fragment_activitytracker_activity_addSessionButton"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:clipToPadding="false"
            android:paddingBottom="16dp"
            tools:listitem="@layout/fragment_activitytracker_activity_session_item"/>
    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>
    <!-- Add Session Button -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fragment_activitytracker_activity_addSessionButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="Add Session"
        android:src="@drawable/ic_button_add"
        android:backgroundTint="@color/colorPrimary"
    app:layout_constraintBottom_toBottomOf="parent"
    app:layout_constraintEnd_toEndOf="parent"
    android:layout_margin="16dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>